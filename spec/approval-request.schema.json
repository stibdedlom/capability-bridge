{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SdlApprovalRequest",
  "description": "A human decision envelope for a risky SDL action (Phase 2 approval envelope).",
  "type": "object",
  "required": [
    "contract_version",
    "request_id",
    "task_frame_ref",
    "risk_tier",
    "scope",
    "expires_at",
    "evidence_refs",
    "scope_hash"
  ],
  "properties": {
    "contract_version": {
      "type": "string",
      "const": "0.3.0"
    },
    "request_id": {
      "type": "string",
      "description": "Stable identifier for this approval request."
    },
    "task_frame_ref": {
      "type": "string",
      "description": "Reference to the task frame that generated the request."
    },
    "risk_tier": {
      "$ref": "#/$defs/risk_tier"
    },
    "scope": {
      "type": "string",
      "description": "Scope of the approval."
    },
    "expires_at": {
      "type": [
        "integer",
        "null"
      ],
      "description": "When the request expires, if ever (Unix milliseconds)."
    },
    "evidence_refs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "References to evidence artifacts."
    },
    "scope_hash": {
      "type": "string",
      "description": "Stable hash computed over the approved scope."
    },
    "trace_id": {
      "type": "string"
    },
    "approval_ref": {
      "type": "string"
    },
    "requested_action": {
      "type": "string"
    },
    "prohibited_actions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "confirmation_ritual": {
      "$ref": "#/$defs/confirmation_ritual"
    },
    "state": {
      "$ref": "#/$defs/approval_state"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "risk_tier": {
      "type": "string",
      "enum": [
        "safe",
        "low",
        "medium",
        "high"
      ],
      "description": "Risk classification for an action or tab."
    },
    "confirmation_ritual": {
      "type": "string",
      "enum": [
        "tap-approve",
        "explicit-confirm",
        "biometric",
        "watch-haptic"
      ],
      "description": "The ritual required to confirm an approval request."
    },
    "approval_state": {
      "type": "string",
      "enum": [
        "pending",
        "presented",
        "approved",
        "denied",
        "deferred",
        "expired"
      ],
      "description": "The state of an SDL approval request."
    }
  }
}
